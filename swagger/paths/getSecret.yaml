post:
  summary: Lay một secret 
  tags: 
    - Secret
  requestBody:
    required: true
    content:
      application/json:
        schema:
          $ref: '../components/schemas.yaml#/SecretCreate'
  responses:
    '201':
      description: Secret được tạo thành công
      content:
        application/json:
          schema:
            $ref: '../components/schemas.yaml#/SecretCreated'
    '400':
      description: Thiếu nội dung
